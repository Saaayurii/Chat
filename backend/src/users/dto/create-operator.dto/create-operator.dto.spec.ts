import { CreateOperatorDto } from './create-operator.dto';

describe('CreateOperatorDto', () => {
  it('should be defined', () => {
    expect(new CreateOperatorDto()).toBeDefined();
  });
});
